Validates email (min 6 chars). Checks for @ (once only), valid domain (. or .com etc) & no spaces/uppercase/weird characters. Prints "Invalid email" for errors (fix code to print "Valid email" for success).

